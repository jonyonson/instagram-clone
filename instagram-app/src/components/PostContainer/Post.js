import React from 'react';
import PropTypes from 'prop-types';

const Post = props => {
  return <div className="PostContainer__Post" />;
};

Post.propTypes = {
  post: PropTypes.array,
};

export default Post;
